name: Auto Update

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Commit and Push Changes
        run: |
          git config --global user.name "obsidien420"
          git config --global user.email "callumdewar44@gmail.com"
          git add .
          git commit -m "Automated Update"
          git push origin main
